using UnityEngine;

//This is here to be labled on every sealed cup prefab

public class SealedCup : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
